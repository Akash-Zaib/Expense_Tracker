import 'package:flutter/material.dart';

class AppColors {
  // Primary & Background
  static const Color primary = Color(0xFF1E3A8A); // Deep blue button
  static const Color background = Color(0xFFF4F6F9); // Light grayish app background
  static const Color cardBackground = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A); // Dark text
  static const Color textSecondary = Color(0xFF6B7280); // Gray text
  static const Color textLight = Colors.white;
  static const Color textMuted = Color(0xFF9CA3AF);

  // Expense Categories / Status Colors
  // Green = My / Personal expenses
  static const Color greenLight = Color(0xFFE8F5E9);
  static const Color greenDark = Color(0xFF4CAF50);

  // Red = Other user (Badar) expenses
  static const Color redLight = Color(0xFFFFEBEE);
  static const Color redDark = Color(0xFFF44336);

  static const Color blueLight = Color(0xFFE3F2FD);
  static const Color blueDark = Color(0xFF2196F3);

  static const Color purpleLight = Color(0xFFF3E5F5);
  static const Color purpleDark = Color(0xFF9C27B0);

  // Border & Inputs
  static const Color border = Color(0xFFE0E0E0);
  static const Color inputBackground = Colors.white;
}
